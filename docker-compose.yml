version: '3'
services:

  skitter-proxy:
    build: proxy/
    depends_on:
      - skitter
      - skitter-auth
    ports:
      - "80:80"
      - "443:443"

  skitter:
    build: webserver/

  skitter-auth:
    build: auth/
    depends_on:
      - skitter-auth-db

  skitter-auth-db:
    image: mariadb
    environment:
      - MYSQL_ROOT_PASSWORD=skitter_auth_dbpass
